#include "monty.h"

/**
 * nop - a placeholder for alignment purpose.
 * @head: a pointer to the head of stack.
 * @line_count: line number in file.
 *
 * Return: no return.
 */
void nop(stack_t **head, unsigned int line_count)
{
	(void)head;
	(void)line_count;
}
